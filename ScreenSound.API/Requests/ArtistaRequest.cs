namespace ScreenSound.API.Requests;

public record ArtistaRequest (string nome, string bio);

/*
 
 * DTO é a sigla referência para Data Transfer Objects ou Objetos de Transferência de Dados, e eles são utilizados nos projetos para transferir
 * dados da camada da aplicação para a camada de apresentação.
 
 * O DTO é um objeto que define como os dados serão transmitidos através da rede, isolando as camadas de dados e permitindo que elas tenham acesso
 * somente ao que foi determinado.
 
 */